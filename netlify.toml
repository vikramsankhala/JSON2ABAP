# Netlify configuration for JSON to ABAP Converter
# Deploy: Connect this repo to Netlify for automatic deploys

[build]
  publish = "."
  command = "echo 'Static site - no build required'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
